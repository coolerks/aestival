# Aestival
应用名称：Aestival
应用图标：frontend/src/assets/icons/application/logo.svg
状态栏/任务栏中的图标：frontend/src/assets/icons/application/icon-template.svg
应用内需要展示图标的地方：frontend/src/assets/icons/application/icon.svg

## 背景
当前项目使用wails3+react+ts初始化的一个空项目，我要做一个桌面端跨平台的 ai agent 应用
先采用tailwindcss+shadcn/ui+zustand+lucide-react来做UI

## 设计

应当给全局禁用右键菜单，而是用shadcn/ui的Dropdown 统一搞一个右键菜单，注意不同区域的右键菜单弹出项不一样，每个菜单项都必须带有icon，且支持多级菜单，如果某项有对应的快捷键，例如复制、粘贴这种，需要在右侧展示快捷键，如果某个位置右键菜单项特别多，还需要对右键内容进行分组

你要按照功能列表，将所有的页面的所有的组件都要实现

软件定位为本地应用，无需做登录之类的页面

### 组件选择

这是 shadcn/ui 完整的组件列表，其中包含了很多 ai 桌面端程序所用到的组件，你可以完全照用，禁止你自己造组件去实现，应当完全用 shadcn/ui 中提供的组件完成UI设计

Attachment
Bubble
Marker
Message
Message Scroller

Accordion
Alert
Alert Dialog
Aspect Ratio
AttachmentNew
Avatar
Badge
Breadcrumb
BubbleNew
Button
Button Group
Calendar
Card
Carousel
Chart
Checkbox
Collapsible
Combobox
Command
Context Menu
Data Table
Date Picker
Dialog
Direction
Drawer
Dropdown Menu
Empty
Field
Hover Card
Input
Input Group
Input OTP
Item
Kbd
Label
MarkerNew
Menubar
MessageNew
Message ScrollerNew
Native Select
Navigation Menu
Pagination
Popover
Progress
Radio Group
Resizable
Scroll Area
Select
Separator
Sheet
Sidebar
Skeleton
Slider
Sonner
Spinner
Switch
Table
Tabs
Textarea
Toast
Toggle
Toggle Group
Tooltip
Typography


### 样式
默认展示2栏，至多3栏，建议采用分割面板
进入页面，默认侧栏不收，右侧为内容区域

侧栏分为3部分，第一部分为顶部菜单区域，第二部分为项目区域，第三部分为应用图标

#### 左侧栏
第一部分，常驻的侧栏菜单 Sidebar：
第一行为一个tabs，包含：Agent（代理）、Chat（聊天）
新建任务
知识库
应用
能力
任务

第二部分，下方是一个个的项目列表，默认收起
点击项目列表的某项后，就展示该项对应的一个个的会话列表，默认5个，最后放一个展开更多，点击后追加5个
需要单独固定一个名称为“任务”的项目，该项目含义为没有选择项目进行对话时，默认会将会话放到这个项目里
例如，新打开页面时，默认相当于这个任务

第三部分，最底部，显示应用图标（frontend/src/assets/icons/application/icon.svg）、应用名称。由于该应用为一个桌面端本地agent应用，且不提供任何的登录、注册页面
因此，这一部分本身为用户头像+用户名称展示的区域，所以就在这里展示应用名称
点击后展示一部分dropdown

#### 中间
新建任务：出现新建任务的页面，中心展示当前项目的信息，底部展示输入框。
内置一堆诗句，不同时间显示的欢迎语不一样，同一时间多个欢迎语来切换，欢迎语就用诗句，诗句选择在清代以前
输入框分为两部分，上半部分输入文字，下半部分展示状态
上半部分：默认展示2行，最多8行
下半部分：分为左、右两部分
左半部分：加号，加号旁边为权限，分为请求审批、自动审批、绕过审批，权限旁边为智能体选择
加号：点击展示dropdown，支持引用文件、文件夹、图片、计划模式、目标模式、初始化、MCP（用于展示状态）、选择技能
右半部分：选项按钮（dropdown，包含全屏输入、复制、剪切、清空）、上下文信息（展示位圆环）、模型切换、发送按钮
输入框中，最开始位置输入斜杠 / 可以出现一些快捷操作，上下文压缩、计划模式、目标模式、分叉、会话统计、选择技能

聊天时，内容区域：
顶部为标题栏，中间为内容区域，下方为输入框
整个标题栏独占一行，右侧与Mac的红绿灯在同一行，且收起、展开侧栏不受影响，标题栏上固定展示红绿灯、收起/展开边栏图标、搜索按钮图标
标题栏分为左右两部分，左边为标题栏，右边为展开侧栏、底栏按钮

#### 右侧栏
右侧栏展开后，标题栏上有一个更多按钮，点击后展示dropdown，内容包括：文件、终端、搜索（这是针对的是文件内容的搜索）、日志、会话调试（就是查看每条消息与ai交互的过程）
可以做成类似于vscode的那种分割面板，如果没有打开任何面板，可以展示让其选择其中的一个
文件面板中，使用material icon，图标目前在assets/icons/material文件夹下。双击文件可以打开文件，此时中间的内容区域从就变为了多tab页的形式，用来浏览文件，代码查看/修改的编辑器用monaco editor
还要支持查看常见的二进制文件
文件后缀到图标的映射应参照 docs/文件图标映射.md
聊天的tab始终不能够被关闭
右侧按钮点击后可以展示侧栏，侧栏中可以展示文件树、终端
展开底栏时，默认打开终端，底栏展示成多tab的形式，点击加号，可以选择跟右侧栏一样的内容：内容包括：文件、终端、搜索（这是针对的是文件内容的搜索）、日志、会话调试（就是查看每条消息与ai交互的过程）

## 功能页面
代理/聊天、知识库、搜索、应用、能力、任务

## 代理/聊天功能
聊天模式下，禁用任何工具
基础的功能
功能：
+ 流式输出
+ 工具调用
+ 切换模型
+ 发送图片
+ 支持读取记忆、写入记忆
+ 发送文档
+ 网络搜索
+ 选择智能体
+ Token 估算
+ 知识库检索
+ 触发工作流
+ Mermaid 预览、下载
+ 图片和 SVG 预览、下载
+ 支持导出 Markdown、HTML、PDF、Word
+ 支持思考，展示思考大纲
+ 会话分叉
+ 聊天复制、重新生成、朗读
+ 支持语音输入
+ 支持多模型同时对话
+ 支持自动上下文压缩
+ 支持选择上下文大小
+ 支持显示当前对话统计信息（模型、条数、输入 token、输出 token、显示花费、支持跨段计费）
+ 支持临时会话
+ 会话管理（支持重命名、删除、归档、搜索、Star）

### 知识库

用嵌入模型生成向量并存入数据库，也支持普通的数据库，并能管理连接
+ PostgreSQL：支持向量、普通的关系关系性数据库检索
+ MySQL：支持向量、普通的关系关系性数据库检索
+ Milvus
+ Weaviate 
+ Chroma 
+ Elasticsearch 
+ Redis
+ Oracle：支持向量、普通的关系关系性数据库检索
+ 文件

### 搜索

悬浮搜索，用 Command 组件即可，实现全局功能、会话、聊天记录搜索

### 应用
在ai输出的 HTML + CSS + JavaScript 代码块上方添加一个按钮，将其添加到应用中心，页面中也支持自定创建
打开应用时，支持在新窗口打开，wails3 支持多窗口
+ 添加应用
+ 设置应用图标
+ 支持修改代码
+ 支持新建应用，自己写进去代码

### 能力

进去之后，多 tab
包含 MCP、Skill、智能体、指令、Hooks

#### MCP

管理 MCP
+ 手动安装
+ AI 一键安装
+ MCP 市场

类型
+ 命令（stdio）
+ HTTP


#### Skill

支持：
+ 创建
+ 查看
+ 修改
+ 删除
+ Skill 市场，用来发现一些技能，例如直接用 https://www.skills.sh 的接口获取
+ AI 创建 Skill
+ AI 一键安装 Skill
+ 导入 Skill（Claude Code、Codex）

#### 智能体

自定义智能体
+ 支持 选择工具范围、Skill 范围、指令范围、Hooks
+ 介绍智能体、可以给足约束
+ 支持 placeholder 提示替换

#### 指令

指令管理，预定义好的 Prompt，能注入到输入框
+ 创建
+ 修改
+ 删除

#### Hooks

支持在不同的阶段进行拦截/注入
SessionStart
    ↓
UserPromptSubmit
    ↓
SubagentStart
    ↓
PreToolUse
    ↓
PermissionRequest（需要授权时）
    ↓
工具执行
    ↓
PostToolUse
    ↓
PreCompact / PostCompact（上下文压缩时）
    ↓
SubagentStop
    ↓
Stop
    ↓
SessionEnd

### 任务
支持创建定时任务，支持页面上点击选择触发时间，也支持自己写 corn 表达式

### 设置
按照不同的分类聚合设置
+ 模型管理
	+ 供应商配置，同一个供应商支持添加多次取别名
		+ OpenRouter
		+ Ollama
		+ Anthropic
		+ OpenAI
		+ Gemini
		+ MiniMax（中国/国际）
		+ Groq
		+ Poe
		+ Huggingface
		+ 硅基流动
		+ 智谱（Z.AI）
		+ 阿里云百炼
		+ 腾讯云
		+ 阶跃星辰
		+ 火山引擎
		+ 月之暗面
		+ DeepSeek
		+ 自定义（兼容 Chat Completions、Response API、Messages API）
	+ 模型配置，从供应商中加模型
+ 信息统计
  + 对会话信息统计，用各种图展示
  + Profile contributions graph 要展示类似于 GitHub 里的贡献图/日历热力图
  + 工具调用的记录也要展示一下
+ 连接：类似于 Open Claw 可被外部控制及交互
  + Telegram 
  + 飞书 
  + Discord
  + 钉钉
  + 微信
  + QQ
  + 连接外部平台不等于登录 Aestival；凭据只保存在本地安全存储或 Secret 引用中
  + 同一平台支持添加多个机器人/应用实例并设置别名
  + 私聊默认配对或允许列表，群聊默认禁用；启用后默认允许列表并要求提及机器人
  + 可以配置默认项目、聊天/代理模式、智能体、模型、会话隔离和通用控制命令
  + 外部代理请求继续遵守 Aestival 的工具范围与审批策略，不允许远程绕过高风险审批
  + 支持查看配对请求、连接健康、能力探测、活动记录与脱敏诊断
  + 微信等依赖外部连接器的渠道必须显示来源、实际能力、限制与实验性状态，不伪装为稳定官方能力
+ 通知
+ 外观设置
	+ 深色/浅色模式
    + 主题
    + 动画，支持开启、关闭
	+ 全局字体
	+ 代码字体
    + 支持自定义主题
+ 快捷键配置
  + 这个要多
+ 关于



material icon 图标来自于此仓库 https://github.com/material-extensions/vscode-material-icon-theme
