# Aestival 当前状态

> 最后更新：2026-07-26 00:02  
> 时区：Asia/Shanghai  
> 当前阶段：阶段 0–1（基础与应用外壳）待验收  
> 代码实施：UI-001、BE-001 已完成实现与验证

## 1. 当前结论

Aestival 已获得用户明确授权进入 UI 实现。阶段 0–1 的前端基础、应用外壳、新建任务首屏和 Wails 桌面窗口配置已经完成实现与验证；第四轮标注涉及的整列毛玻璃透明度、Sidebar 状态层次、全高边界和左上方向窗口缩放冲突已经修复；第五轮标注涉及的侧栏收起时标题栏左段与侧栏回缩不统一问题已经修复，标题栏控件位置不再随侧栏状态变化。业务数据均明确标记为 Mock，未接入模型、工具、文件、知识库或任务调度业务服务。

## 2. 已完成

| 任务 | 结果 |
| --- | --- |
| DOC-001 | 完成 `docs/设计方案/` 下 12 份 UI 设计文档 |
| DOC-002 | 明确 Card 非默认容器、禁止 Card/视觉卡片嵌套 |
| DOC-003 | 建立 `docs/协作同步/` 跨会话同步体系 |
| DOC-004 | 创建项目根目录中文 `AGENTS.md` |
| UI-001 | 完成 Tailwind CSS + shadcn/ui + Zustand 基础、60% 半透明整列 Sidebar 材质层、清晰的 hover/选中状态、连续全高边界、侧栏收起时标题栏与侧栏同步回缩、Command、ContextMenu、Resizable/Sheet 工作区、新建任务首屏和 Mock 输入反馈 |
| BE-001 | 完成 Wails 主窗口名称、1440×900 默认尺寸、960×640 最小尺寸、macOS 隐藏式统一标题栏、46px 原生控件区域、透明窗口背景与 translucent backdrop 配置 |
| QA-001 | 通过浏览器视觉/交互回归、精确项目产物 `bin/aestival.app` 原生窗口检查、TypeScript/Vite production build、Go 测试、`wails3 build` 和 `wails3 package` |

## 3. 进行中

当前无进行中任务。

## 4. 待验收

| 任务 | 范围与结果 |
| --- | --- |
| UI-001 | 前端基础、应用外壳、新建任务首屏和五轮用户标注调整已实现；等待用户再次验收 |
| BE-001 | Wails 桌面主窗口 UI/窗口外壳已实现；标题栏拖拽区已避让原生缩放边缘，精确项目 `.app` 已完成原生外观和缩放后稳定状态检查，未添加业务服务 |

## 5. 下一步

| 任务 | 范围 |
| --- | --- |
| UI-002 | 实现代理/聊天消息流、运行状态、工具调用展示与审批交互，继续使用独立 Mock 服务层 |

## 6. 已知限制

- 知识库、应用、能力和任务页面当前只有可导航页面骨架。
- production bundle 有大于 500 kB 的提示，后续按页面和能力域拆包。
- 当前 Node.js 20.10.0 低于 shadcn 4.14.1 CLI 声明的最低版本 20.18.1；已生成组件与 production build 可正常工作，后续再次运行 CLI 前应升级 Node.js。
- Computer Use 可以执行原生窗口缩放并检查缩放后的稳定画面，但不会记录拖动过程的每一帧；UI-001 最终验收前仍建议人工连续拖动左上角观察是否存在极短促微抖。
