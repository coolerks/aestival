# Aestival 当前状态

> 最后更新：2026-07-26 16:06
> 时区：Asia/Shanghai  
> 当前阶段：阶段 0–1（基础与应用外壳）待验收  
> 代码实施：UI-001、BE-001 已完成实现与验证

## 1. 当前结论

Aestival 已获得用户明确授权进入 UI 实现。阶段 0–1 的前端基础、应用外壳、新建任务首屏和 Wails 桌面窗口配置已经完成实现与验证；第四至第六轮标注涉及的整列毛玻璃、Sidebar 状态与边界、侧栏同步回缩问题已经修复。第八轮通过移除 Wails `InvisibleTitleBarHeight` 强制拖拽根治了顶部角落缩放抖动/跑位；第十轮接入 Wails 前端运行时；第十一轮把拖拽语义放到完整标题栏，仅让四个真实按钮保持 `no-drag`，消除了大范围容器遮挡形成的窄条拖动问题；第十二轮禁止应用外壳文字框选、为主内容和编辑表面建立显式选择边界，并把标题栏拖动光标固定为普通箭头；第十三轮接入 Wails `ToggleMaximise`，使标题栏空白拖动区双击可最大化/恢复；第十四轮在每次切换后主动恢复 Wails 尺寸约束，修复从最大化状态直接拖角时可无限缩小的问题。本轮新增单一全局标题栏规范，并按用户图示把页面、会话或文件名称放到侧栏右侧的主内容标题栏段；侧栏材质段只保留原生窗口控件与侧栏开关，管理页骨架不再重复同名标题或创建第二条带底边页头。角落缩放和整条标题栏拖动均已由用户实测确认，最新窗口与单标题栏调整等待用户原生窗口复核。业务数据均明确标记为 Mock，未接入模型、工具、文件、知识库或任务调度业务服务。

## 2. 已完成

| 任务 | 结果 |
| --- | --- |
| DOC-001 | 完成 `docs/设计方案/` 下 12 份 UI 设计文档 |
| DOC-002 | 明确 Card 非默认容器、禁止 Card/视觉卡片嵌套 |
| DOC-003 | 建立 `docs/协作同步/` 跨会话同步体系 |
| DOC-004 | 创建项目根目录中文 `AGENTS.md` |
| UI-001 | 完成 Tailwind CSS + shadcn/ui + Zustand 基础、60% 半透明整列 Sidebar 材质层、清晰的 hover/选中状态、连续全高边界、侧栏收起时标题栏与侧栏同步回缩、全局单标题栏、Command、ContextMenu、Resizable/Sheet 工作区、新建任务首屏和 Mock 输入反馈 |
| BE-001 | 完成 Wails 主窗口名称、1440×900 默认尺寸、持续有效的 960×640 最小尺寸、macOS 隐藏式统一标题栏、透明窗口背景、translucent backdrop、前端全标题栏拖动、双击最大化/恢复与外壳/内容文本选择边界 |
| QA-001 | 通过浏览器视觉/交互回归、管理页单标题栏矩阵、精确项目产物 `bin/aestival.app` 原生窗口检查、用户原生拖动/缩放实测、标题栏拖动无文字选区检查、标题栏双击/按钮命中边界检查、TypeScript/Vite production build、Go 测试、Go vet 和 `wails3 package` |

## 3. 进行中

当前无进行中任务。

## 4. 待验收

| 任务 | 范围与结果 |
| --- | --- |
| UI-001 | 前端基础、应用外壳、新建任务首屏、多轮用户标注与全局单标题栏规范已实现；名称已按图示移到侧栏右侧的主内容标题栏段，等待用户再次验收 |
| BE-001 | Wails 桌面主窗口 UI/窗口外壳已实现；四边/四角继续由 AppKit 原生缩放，完整标题栏除真实按钮外均可拖动，双击空白拖动区可最大化/恢复；最大化后从角落直接恢复仍应受 960×640 最小尺寸限制；等待用户复核原生拖动、双击与约束保持，未添加业务服务 |

## 5. 下一步

| 任务 | 范围 |
| --- | --- |
| UI-002 | 实现代理/聊天消息流、运行状态、工具调用展示与审批交互，继续使用独立 Mock 服务层 |

## 6. 已知限制

- 知识库、应用、能力和任务页面当前只有可导航页面骨架。
- production bundle 有大于 500 kB 的提示，后续按页面和能力域拆包。
- 当前 Node.js 20.10.0 低于 shadcn 4.14.1 CLI 声明的最低版本 20.18.1；已生成组件与 production build 可正常工作，后续再次运行 CLI 前应升级 Node.js。
