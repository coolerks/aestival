# Aestival 协作变更日志

## 2026-07-26 20:13（Asia/Shanghai）— UI-008：知识库、数据连接与全局搜索 Mock UI

### 范围与边界

- 按 `05-知识库与全局搜索.md` 实现知识库管理页，不在内容区重复“知识库”页面名称，继续使用全局单标题栏。
- 连接、测试、创建、同步、检索、token 与费用均为明确的前端 Mock；不读取文件正文，不访问数据库/向量库，不调用嵌入或重排模型，不写本地索引，不保存密码、Token 或环境变量值。

### 修改

- `mock-knowledge.ts`、`knowledge-store.ts`：建立九类数据源、连接、知识库、内容、检索结果和同步记录数据，以及独立 Zustand 页面状态与创建、删除、断开、同步、检索状态机。
- `components/knowledge/`：实现知识库、数据连接、检索测试、同步记录四个页签；知识库默认 DataTable、可切换独立对象 Card 网格，指标使用紧凑指标带且无 Card 嵌套。
- 实现知识库五页签详情和连接五页签详情；内容表、检索配置、权限、诊断、关联知识库和同步历史均可交互。
- 实现四步数据连接向导和五步知识库向导；桌面使用固定标题/滚动内容/操作栏 Dialog，窄窗口根据 `useNarrowWorkspace` 切换 Drawer；包含 Field、Accordion、Progress、Slider + Input、Carousel 和明确的敏感信息边界。
- 实现检索参数、结果/原始 JSON/查询过程切换、同步失败重试和历史清理；删除知识库、断开连接与本地索引选择使用 AlertDialog。
- `global-command.tsx` 加入聊天记录与知识库结果，`#` 前缀可直接打开知识库详情；`placeholder-page.tsx` 按需加载知识库页并增加页面级错误边界。
- 新增官方 shadcn `Accordion`、`Breadcrumb`、`Carousel`、`Drawer`、`Item`、`Pagination`、`Slider` 组件及 `embla-carousel-react` 依赖；调整中文无障碍文案和语义 token。
- 用户最新侧栏标注同步修正：项目会话已经全部显示时，“已显示全部”不再渲染下拉图标，“展开更多”仍保留图标。

### 验证

- 浏览器标准窗口验证四个顶层页签、知识库内容详情、连接详情、四步连接创建、五步知识库向导切片预览、Mock 检索结果和全局 `# 产品文档` 搜索跳转；创建连接后会出现在列表并打开详情。
- 网格模式共 4 个独立 Card，嵌套 Card 计数为 0；页面 `document/body` 均无全局横向溢出。所有表格外层已使用局部横向滚动，页签支持窄宽度滚动；窄窗口向导由源码与媒体查询分支确认使用 Drawer。
- 修复首次页面运行的 Zustand 派生数组选择器无限更新，以及 720px 高窗口下九类来源挤出 Dialog 操作栏的问题；页面错误边界可防止按需页面异常拖垮整个应用。
- `npm run build`、`git diff --check`、业务组件硬编码色/原生弹窗/`space-*` 扫描通过。production：主 chunk 约 742 kB，知识库按需 chunk 约 154 kB；仍只有既知的主 chunk 大于 500 kB 提示。

### 限制与下一步

- 未接真实数据契约、Secret 存储、文件选择器、连接能力探测、索引、检索、同步或计费服务；等待后端业务方案讨论。
- 下一项可直接执行任务：UI-009（应用中心管理页 Mock）。

## 2026-07-26 19:41（Asia/Shanghai）— UI-001、UI-007：标题栏、会话侧栏与消息操作标注修正

### 用户反馈与修改

- `app-titlebar.tsx` 改为明确的侧栏控制列与主标题列；左段不显示名称，主标题后直接显示会话更多菜单。多模型比较、会话统计、压缩上下文从正文顶部移入该菜单，正文不再保留重复工具栏。
- `app-sidebar.tsx` 移除侧栏会话搜索框；打开已有会话或发送首条消息后，“新建任务”不再保持选中，新会话所属项目自动展开。
- `session-project-group.tsx` 移除项目选中态和全部项目操作；项目本身不支持归档。会话 hover/选中背景扩展为与项目行相同的 240px，内容保留 32px 层级缩进；相对时间改由 HoverCard 展示。
- 每条会话只在 Hover 或操作按钮键盘聚焦时显示归档与更多两个图标；归档使用 AlertDialog 确认并只更新会话 Mock 状态，更多复用统一会话菜单。
- `conversation-message.tsx` 将复制、编辑/重新生成、朗读和更多操作改为消息边界外的绝对定位浮层；默认隐藏，Hover 或操作按钮键盘聚焦时显示，不参与 Bubble/消息本体高度计算。

### 验证

- 浏览器 DOM 与计算样式确认：项目行和会话行均为 `left=8px, width=240px`，会话内容 `padding-left=32px`；当前会话具有 `data-active`，新建任务与项目行没有误选中。
- 发送新的 Mock 任务后，新会话立即进入“任务”项目并成为唯一选中行；侧栏不存在重复搜索框。
- 项目归档按钮计数为 0；会话归档按钮 Hover 时显示、离开后父层 `opacity=0`，可打开标题匹配的 AlertDialog 并取消。
- 用户消息 Bubble 与外层容器高度均为 48px；操作层为 `position:absolute`，位于 Bubble 下方 4px，Hover 时 `opacity=1`，不抬高消息高度。
- 标题栏更多菜单依次包含多模型比较、会话统计、压缩上下文，并继续提供 Star、重命名、分叉、移动、定时任务、导出、归档与删除，统计入口没有重复。
- React/shadcn 复核、`git diff --check` 与 `npm run build` 通过；浏览器无新增 error，只有 Wails 浏览器预览模式的既知提示。production 主 chunk 约 720 kB，继续作为非阻塞拆包项。

### 下一步

- UI-008：继续知识库首页、数据连接、创建流程、详情与检索测试的前端 Mock。

## 2026-07-26 19:09（Asia/Shanghai）— UI-007：会话管理与定时任务 Mock UI

### 范围与边界

- 完成会话列表与管理动作，并统一侧栏右键、会话右上角菜单、快捷键和全局 Command 的名称与状态。
- 搜索、Star、排序、归档、重命名、移动、删除、导出预设与定时任务均为前端内存 Mock；不会写入本地会话数据库、删除文件或注册真实调度器。

### 修改

- `frontend/src/data/mock-session-management.ts`、`workspace-store.ts`：新增稳定会话 ID、项目归属、Star、归档、状态、相对时间、可见数量、管理 Dialog 和定时任务记录；新会话、临时会话保存与分叉会同步进入列表。
- `session-project-group.tsx`、`app-sidebar.tsx`：固定“任务”项目第一项，按 Star 优先和最近活动排序；每个项目首批 5 条、每次再加载 5 条，结束显示“已显示全部”；加入状态、相对时间、搜索和归档过滤。
- `session-menu-content.tsx` 与四个管理 Dialog：实现打开、Star、重命名、分叉、移动、创建定时任务、统计、导出、归档和危险删除；运行中/待审批删除使用对应确认文案。
- `scheduled-task-dialog.tsx`：使用 Field、Select、ToggleGroup、Checkbox、Switch、Alert 和 Progress 实现基本信息、执行内容、触发时间、运行策略、通知与确认五步流程，固定显示 Asia/Shanghai 时区和前端 Mock 边界。
- `global-command.tsx`、`conversation-view.tsx`、`App.tsx`：把当前会话动作和可搜索会话接入 Command；会话右上角复用同一菜单；接入 F2、Star、归档、删除、定时任务、统计、分叉与导出快捷键。
- `popover.tsx`、`prompt-composer.tsx`：受控 Slash Popover 改用 textarea ref 作为定位锚点，不再把 textarea 伪装成按钮；保留原生 textarea 语义和斜杠菜单定位。

### 验证

- 浏览器确认“任务”项目首批显示 5 条，点击“展开更多”后显示剩余会话和“已显示全部”；搜索“窗口”跨项目返回 3 条匹配结果，Star、审批、失败和相对时间状态可读。
- 侧栏右键与会话菜单均显示一致的 10 类动作；重命名 Dialog、移动项目 Command、删除 AlertDialog 和五步定时任务向导均可键盘操作；全局 Command 同时展示当前会话动作和动态会话列表。
- 标题名称仍唯一位于主标题栏：侧栏宽 256px 时名称 x=272px；900×640 与 1600×900 均无横向溢出，Card 嵌套计数为 0。
- 输入框最终为原生 `TEXTAREA`，没有伪按钮 role；输入 `/` 后 Slash Command 正常打开。修复后没有新增浏览器控制台错误。
- React/shadcn 复核、`npm run build`、`git diff --check`、隔离 `GOCACHE` 的 `go test ./...`、`go vet ./...`、`wails3 build` 与 `wails3 package` 通过；`bin/aestival.app` 已更新。
- production：主 chunk 约 711 kB；会话管理 Dialog 约 15 kB、会话视图约 52 kB、上下文图表约 306 kB，均为按需 chunk。Node engine 与主 chunk 提示仍为非阻塞已知项。

### 下一步

- UI-008：实现知识库首页、数据连接、创建流程、详情与检索测试的前端 Mock。

## 2026-07-26 18:13（Asia/Shanghai）— UI-001：标题移入主标题栏段

### 用户反馈与修改

- 按最新图示纠正标题位置：左侧栏对应的标题栏区域只保留 macOS 窗口控件安全区与侧栏开关，不再显示“新建任务”等名称。
- `AppTitlebar` 从悬浮绝对定位改为明确的左右两段 Flex 布局；展开时右段从 256px 侧栏边界后开始，标题保留 16px 左内边距。
- 侧栏完全收起时，标题随主标题段移动到 140px 的安全位置；再次展开后恢复到 272px，不产生重复标题。
- 修复 `SidebarTrigger` 内嵌 `IconButton` 形成的 button 套 button，改为单个 shadcn Button 作为 TooltipTrigger，保留可访问名称与 Tooltip。

### 验证

- 应用内浏览器 1280×720 实测：侧栏边界 x=256px，标题 x=272px，间距精确为 16px；收起时侧栏占位为 0px、标题 x=140px，恢复后重新为 272px。
- 标题栏聚焦对照：`docs/协作同步/design-qa-title-placement-comparison.png`；实现全屏：`docs/协作同步/design-qa-title-placement-1280x720.png`。
- 浏览器可访问树确认标题只在全局 banner 中渲染一次；侧栏触发器为单个按钮，未再产生嵌套按钮错误。
- `npm run build`、`go test ./...`、`go vet ./...`、`wails3 package` 与 `git diff --check` 通过；`bin/aestival.app` 已更新。production 仍仅有既知的 Node engine 与主 chunk 大于 500 kB 提示。

## 2026-07-26 18:01（Asia/Shanghai）— UI-006：AI 代码加入应用与朗读 Mock UI

### 范围与边界

- 实现完成态 AI 消息中的 HTML、CSS、JavaScript 代码组合、预览、加入应用配置和朗读控制。
- 代码、预览、应用草稿、权限、语音、语速与播放进度均为前端 Mock；不会写入文件、应用数据库或系统权限，不调用模型、系统语音或业务后端。

### 修改

- `frontend/src/data/mock-ai-app.ts`：新增三文件应用样例、受限预览文档组合、应用草稿/权限和朗读状态类型。
- `frontend/src/components/chat/ai-code-bundle.tsx`：使用完整 shadcn Card 结构展示文件 Tabs、可选择源码、复制、保存 Mock、预览和加入应用入口。
- `frontend/src/components/chat/ai-code-preview-dialog.tsx`：使用 Dialog + Tabs + ScrollArea 展示源码和独立来源的 `sandbox="allow-scripts"` 固定样例 iframe；样例不包含网络、文件或剪贴板访问。
- `frontend/src/components/chat/ai-app-create-dialog.tsx`：预填名称、说明、代码、来源与模型，要求确认默认应用图标、入口 HTML、窗口尺寸、网络策略、文件和剪贴板权限；全部网络显示风险 Alert，完成后明确为未持久化草稿。
- `frontend/src/components/pages/mock-app-draft-page.tsx`：提供可从会话打开的内存草稿代码/预览/权限三页，并把应用名称只显示在全局标题栏。
- `frontend/src/components/shell/speech-mini-player.tsx`：实现跨页面保留的播放/暂停/停止、进度、语音和 0.75–1.5 倍语速控制；状态对象集中在 Zustand。
- `conversation-message.tsx`、`conversation-view.tsx`、`workspace-shell.tsx`、`placeholder-page.tsx`、`app-titlebar.tsx` 与 `workspace-store.ts` 接入完整状态链；朗读控制条和应用草稿页使用动态导入。

### 验证

- 浏览器完成态会话显示 `index.html`、`styles.css`、`script.js` 三个 Tabs；每个 Tab 有对应 tabpanel，源码表面保持可选择。
- 预览 Dialog 可访问标题、受限说明、源码/预览 Tabs 和 iframe；可访问树确认 iframe 内渲染标题、计数和按钮，不与 Aestival DOM/存储共享来源。
- 应用创建 Dialog 默认权限全关；未确认图标时创建按钮禁用，选择“全部网络”显示高风险 Alert；确认后生成 Mock 草稿，原消息显示“已生成 Mock 草稿”。
- “打开 Mock 编辑器”进入代码/实时预览/权限页，全局标题栏显示草稿名称，内容区没有重复同名标题；可返回来源会话。
- 消息“朗读”启动全局迷你控制条；切换到知识库后控制条仍存在，语音可改为“清和”、语速可改为 1.5×，暂停/继续/停止均可操作。
- Card 嵌套计数为 0；所有 Dialog、Popover、Tabs、Select、ToggleGroup、Checkbox 与纯图标按钮具有可访问名称和键盘语义。
- React 复核通过：新组件使用命名导出，Hook 无条件调用，定时器清理，列表使用稳定 ID，状态靠近消费边界，重型浮层和草稿页按需加载。
- `npm run build`、`go test ./...`、`go vet ./...` 与使用隔离 `GOCACHE` 的 `wails3 package` 全部通过，产物更新为 `bin/aestival.app`。
- production：主 chunk 约 689 kB；会话视图约 61 kB、上下文图表约 306 kB、朗读控制条约 3 kB、应用草稿页约 4 kB，均为按需 chunk。非阻塞 Node engine 与主 chunk 提示保持。

## 2026-07-26 17:37（Asia/Shanghai）— UI-005：会话分叉、临时会话、版本切换与导出 Mock UI

### 范围与边界

- 实现会话分叉、临时会话、重新生成版本保留和导出预览；所有关系、版本、Token、费用、位置和进度均为前端内存 Mock。
- 导出流程不会创建真实文件，临时会话不会写入历史或记忆，分支不会持久化到项目，重新生成不会调用模型或产生真实费用。

### 修改

- `frontend/src/data/mock-conversation-management.ts`：新增回复版本、分支关系、导出范围/格式/媒体和 Mock 工厂。
- `frontend/src/store/workspace-store.ts`：集中管理临时会话、关闭确认、分叉、导出、回复版本及原会话/分支快照，并在会话重置时清理关联状态。
- 新增 `conversation-fork-dialog.tsx`、`temporary-conversation-dialog.tsx`、`fork-relation-bar.tsx`、`message-version-switcher.tsx` 与 `conversation-export-dialog.tsx`，分别覆盖分叉表单、保存/丢弃确认、双向关系导航、旧版本保留和完整导出预览。
- `conversation-view.tsx`、`conversation-message.tsx`、`prompt-composer.tsx`、`app-titlebar.tsx` 与 `app-sidebar.tsx` 接入消息菜单、`/fork`、临时会话入口/标题栏 Badge/历史隔离、重新生成和不同导出范围。
- 通过 shadcn 官方 registry 新增 `Checkbox`、`Field`、`Label`，复用现有 `Dialog`、`AlertDialog`、`Tabs`、`Progress`、`ToggleGroup`、`Select`、`Input` 和 `Sonner`；Dialog 关闭按钮的无障碍名称统一为“关闭”。

### 验证

- 浏览器回归确认：临时会话不进入普通历史，离开时可取消、丢弃或保存到任务；标题栏显示 Timer Badge，保存后移除。
- 从消息菜单与 `/fork` 均可打开分叉 Dialog；分叉点和项目下拉显示可读名称，继承选项与 Token 估算可见，创建后原会话/分支可双向切换且各自消息快照保持。
- 重新生成后旧回复仍通过版本 Tabs 保留；Tabs 具有对应 tabpanel，可切回旧版并继续创建新版本，费用仅标记为 Mock 估算。
- 导出支持完整会话、当前分支和选定消息，内容、隐私脱敏、媒体、Markdown/HTML/PDF/Word、位置、文件名预览与 Progress 均可操作；完成后 Sonner 明确提示未创建文件。
- 最新 production 构建中 Dialog 关闭按钮为中文，Card 嵌套计数为 0。
- React 复核通过；`npm run build`、`go test ./...`、`go vet ./...` 与使用隔离 `GOCACHE` 的 `wails3 package` 全部通过，产物更新为 `bin/aestival.app`。
- production：主 chunk 约 684 kB；会话视图约 75 kB、上下文图表约 310 kB，均为按需 chunk。非阻塞 Node engine 与主 chunk 提示保持。

## 2026-07-26 17:18（Asia/Shanghai）— UI-004：多模型比较、会话统计与上下文压缩 Mock UI

### 范围与边界

- 实现同一轮次的多模型结果组织、会话统计和上下文压缩事件；用户消息仍只展示一次。
- 模型回复、Token、费用、延迟、计费段、工具结果和压缩摘要均为明确标记的静态 Mock，不调用真实模型、不执行工具、不结算或持久化。

### 修改

- `frontend/src/data/mock-conversation-insights.ts`：新增模型候选回复、统计、跨段计费、工具明细与压缩事件工厂。
- `frontend/src/store/workspace-store.ts`：集中管理多模型展示、统计 Dialog 和压缩事件；新会话/重置时清理对应状态。
- `frontend/src/components/chat/multi-model-response.tsx`：默认使用模型 Tabs，支持宽屏双列 ScrollArea、同轮结果 Token/费用估算和执行模型 Mock 选择。
- `frontend/src/components/chat/conversation-stats-dialog.tsx`：使用 Dialog + Tabs + Table 实现概览、Token、费用、计费段和工具五页；指标使用普通指标带，不生成卡片墙。
- `frontend/src/components/chat/context-compression-event.tsx`：使用 Collapsible + Separator 展示压缩前后 Token、范围、摘要、重新压缩和压缩前分叉 Mock。
- `frontend/src/components/chat/conversation-view.tsx`、`prompt-composer.tsx`：接入比较、统计、压缩入口，并让 `/stats`、`/compact` 操作真实的前端 Mock 状态。
- 通过 shadcn 官方 registry 新增缺失的 `Table` 组件并复核生成文件。

### 验证

- 浏览器 1280×720 完成代理状态链后，多模型页签与双列均可切换；用户消息计数保持 1，同一轮两份模型回复均显示独立延迟、Token 与估算费用。
- 会话统计 Dialog 的五个 Tabs 均可操作；跨段计费 Table 显示价格区间、Token、单价和预计金额，工具页显示次数、成功率与耗时。
- 压缩事件显示 18,420 → 8,940 Token；点击重新压缩或执行 `/compact` 后更新为 15,120 → 7,320 Token，摘要和消息范围同步变化；压缩前分叉明确为 Mock。
- `/stats` 与顶部“会话统计”共用同一 Dialog 状态；Card 嵌套计数为 0。
- React 复核通过：新业务组件命名导出，Hook 无条件调用，状态靠近消费边界，列表使用稳定 ID，Dialog 有标题，TabsTrigger 位于 TabsList，二选一布局使用 ToggleGroup。
- `npm run build`、`go test ./...`、`go vet ./...` 与使用隔离 `GOCACHE` 的 `wails3 package` 全部通过；产物更新为 `bin/aestival.app`。
- production：主 chunk 约 679 kB；会话视图约 50 kB、上下文图表约 343 kB，均为按需 chunk。非阻塞 Node engine 与主 chunk 提示保持。

## 2026-07-26 17:01（Asia/Shanghai）— UI-003：会话输入、Slash、附件与上下文 Mock UI

### 范围与边界

- 完善阶段 2 的会话输入组合：附件、任务模式、Slash Command、智能体/模型、上下文、输入选项与全屏编辑。
- 所有附件均为前端样例，不打开系统文件选择器、不读取文件；模型、智能体、上下文 token 和压缩均为 Mock，不调用服务商或业务后端。

### 修改

- `frontend/src/data/mock-composer.ts`：新增附件、智能体、模型与 Slash Command 的类型、选项和 Mock 工厂；`mock-conversation.ts` 支持用户消息携带附件快照。
- `frontend/src/store/workspace-store.ts`：集中管理审批策略、计划/目标模式、附件、智能体/模型、自动压缩和上下文大小；发送时附件进入用户消息并清空输入区。
- `frontend/src/components/chat/prompt-composer.tsx`：重构输入组合，支持 2–8 行自适应、附件区、Slash 键盘导航/Tab 补全/Esc 关闭、聊天模式禁用、模式标签、运行时停止和完整底部控件。
- 新增 `composer-attachments.tsx`、`slash-command-menu.tsx`、`composer-agent-selector.tsx`、`composer-model-selector.tsx`、`composer-options.tsx`、`context-usage-popover.tsx`；实现附件处理/移除、Command 选择、Chart 圆环、自动压缩 Switch、上下文 Select、全屏共享草稿和清空撤销。
- 通过 shadcn 官方 registry 新增 `Popover`、`Switch`、`Select`、`Chart` 及其 `recharts` 依赖；Attachment registry 会覆盖已有 Button，因此通过 dry-run/view 只加入缺失的官方 Attachment 组件并保留本地 Button。
- Recharts 上下文控件通过 `React.lazy + Suspense` 独立按需加载，避免约 343 kB 图表运行时进入主 chunk。

### 验证

- 浏览器 1280×720：图片附件状态从 `processing` 转为 `done`，可移除；发送后输入附件清零、用户消息附件数量为 1，页面无溢出且 Card 嵌套为 0。
- Slash：输入 `/pl` 只显示 `/plan`，按 Tab 后草稿清空并出现计划模式标签；聊天模式 `/goal` 为 disabled、显示禁用原因、按 Enter 不发送且保留命令。
- 智能体从“通用智能体”切换到“代码审查”、模型从“本地 Mock”切换到“Mock Code”后浮层关闭，选择在进入会话后保持。
- 上下文浮层显示 Chart 百分比、消息/系统/附件/预留输出分项、自动压缩 Switch、上下文 Select 与立即压缩 Mock。
- 全屏输入读取原草稿与附件，编辑后同步回底部输入；清空后正文与附件均归零，点击 Sonner“撤销”后完整恢复。
- React 复核通过：业务组件按文件拆分并命名导出，Hooks 无条件调用，定时器有 cleanup，列表使用稳定 ID，窄屏隐藏文字的按钮均补充 aria-label，Dialog 有标题，Select/Command 项均位于 Group。
- `npm run build`、`go test ./...`、`go vet ./...` 与 `wails3 package` 全部通过；产物更新为 `bin/aestival.app`。
- production：主 chunk 约 675 kB；会话视图约 32 kB、上下文图表约 343 kB，均为按需 chunk。非阻塞 Node engine 与主 chunk 提示保持。

## 2026-07-26 16:35（Asia/Shanghai）— UI-002：代理/聊天消息流与运行审批 Mock UI

### 范围与边界

- 按设计方案阶段 2 实现消息流、运行状态、思考大纲、工具调用和审批交互；所有数据与状态仅存在前端内存，不接模型、工具执行、文件、终端、知识库、费用或持久化业务服务。
- 代理模式演示完整审批链路；聊天模式保留普通消息流，但明确跳过工具与审批。

### 修改

- `frontend/src/data/mock-conversation.ts`：新增消息、工具调用和运行状态的独立 Mock 数据类型与文案。
- `frontend/src/store/workspace-store.ts`：使用 `idle/waiting/thinking/awaiting-approval/streaming/completed/failed/cancelled` 显式状态机，提供发送、审批、完成、停止、重试、打开 Mock 会话和重置动作。
- `frontend/src/components/chat/conversation-view.tsx`、`conversation-message.tsx`、`tool-call-card.tsx`：新增唯一消息滚动区、用户/AI 消息、思考大纲、单层工具调用 Card、审批普通分区、状态、停止/重试、消息操作与区域右键菜单。
- `frontend/src/components/chat/prompt-composer.tsx`：发送草稿进入会话，运行时发送按钮切换为停止；绕过审批使用 `AlertDialog` 明确 Mock 边界。
- `frontend/src/components/shell/app-titlebar.tsx`、`app-sidebar.tsx`、`workspace-shell.tsx`：标题栏显示会话名称和运行/审批状态；侧栏显示当前会话与 Spinner/审批图标；新建任务重置会话；会话视图使用 `React.lazy + Suspense` 按需加载。
- 通过 shadcn 官方 registry 新增 `Alert`、`Card`、`Progress`、`Spinner`，并复核生成文件；Spinner 的默认可访问名称调整为中文。

### 验证

- 浏览器 1280×720 代理链路：发送后标题栏切换为会话名称，约 1.8 秒进入等待审批；页面存在“允许一次/本会话允许/拒绝”，允许后工具与消息均进入完成状态。
- 聊天模式链路：显示“聊天模式，不会调用工具”，工具 Card 和审批按钮数量均为 0；停止后发送按钮恢复并显示重试，重试重新进入思考状态。
- 消息区域只有 1 个 ScrollArea；工具调用 Card 数量为 1、嵌套 Card 数量为 0；文档与 1280×720 视口尺寸一致，无横向或页面级纵向溢出。
- 会话视图首次进入显示结构匹配的 Skeleton，加载后 Skeleton 清零；production 生成约 33 kB 会话按需 chunk，主 chunk 约 643 kB。
- React 复核通过：组件命名导出，Hooks 无条件调用且定时器有 cleanup，列表使用稳定消息 ID，图片有 alt，按钮/菜单保留键盘语义。
- `npm run build`、`go test ./...`、`go vet ./...` 与 `wails3 package` 通过；最终产物更新为 `bin/aestival.app`。
- 非阻塞提示：Node.js 20.10.0 低于部分 shadcn CLI 包声明的最低版本；production 主 chunk 仍大于 500 kB。

## 2026-07-26 15:52（Asia/Shanghai）— UI-001 第八轮修复：名称移入主内容标题栏段

### 用户反馈与规范

- 用户指出名称虽然已经移入全局标题栏，但不应放在左侧栏材质区；应位于侧栏边界右侧的主内容标题栏区域。
- 补充稳定规范：侧栏展开时，侧栏标题栏段只保留原生窗口控件与侧栏开关；当前页面、会话或文件名称从主内容左缘留白处开始。侧栏收起时，名称移动到窗口安全区与侧栏开关之后。

### 修改

- `frontend/src/components/shell/app-titlebar.tsx`：名称改为独立的绝对定位只读层；展开侧栏时使用 `var(--sidebar-width) + 1rem` 对齐主内容，收起时使用 124px 左侧安全位置，同时预留右侧标题栏动作空间并保持拖动事件穿透。
- 同步 `AGENTS.md`、设计总览、视觉系统与 `DEC-012`，明确全局标题栏内部的名称归属和响应式位置。

### 验证

- 浏览器 1280×720 展开回归：侧栏右边界 x=256，名称 x=272，与主内容左缘保留 16px；收起回归：侧栏滑出至 x=-256，名称 x=124，位于右边缘 x=116 的侧栏按钮之后。
- 名称定位层为 `pointer-events: none`，完整标题栏继续保持 Wails `drag`；主内容同名重复计数为 0、`main header` 为 0，文档尺寸与视口均为 1280×720。
- `npm run build`、`go test ./...`、`go vet ./...` 与使用隔离 `GOCACHE` 的 `wails3 package` 全部通过，最终产物已更新为 `bin/aestival.app`。
- 已知非阻塞提示保持不变：Node.js 20.10.0 低于部分 CLI 包声明的最低版本；production 主 chunk 约 631 kB。

## 2026-07-26 15:42（Asia/Shanghai）— UI-001 第七轮修复：全局单标题栏

### 用户反馈与规范

- 用户指出管理页骨架同时存在窗口全局标题栏与内容区带底边页头，形成双标题栏；要求名称统一只在最上方标题栏展示。
- 新增稳定规范：当前页面、会话或文件名称只允许出现在全局标题栏。内容区不得重复同名主标题，也不得用带背景、底边或固定整行布局的 Header 模拟第二条标题栏；普通章节标题、Breadcrumb、内容说明、状态和操作工具栏不受影响。

### 修改

- `frontend/src/components/shell/app-titlebar.tsx`：恢复基于 `activePage` 和 `navigationItems` 派生的当前名称，在全局标题栏显示并保持截断；未新增同步状态或额外 Hook。
- `frontend/src/components/pages/placeholder-page.tsx`：删除管理页骨架中带底边的整行页头、重复的页面名称和“后续阶段”Badge；空状态标题改为通用“页面骨架已接入”，页面说明进入普通内容区。
- 同步 `AGENTS.md`、设计总览、视觉系统、信息架构、知识库页面、验收矩阵和 `DEC-012`，统一后续所有页面的标题归属。

### 验证

- 浏览器 1280×720 回归：知识库页面全局标题栏显示“知识库”，主内容中“知识库”重复计数为 0，`main header` 为 0，内容顶层底边计数为 0；文档尺寸与视口一致。
- 知识库、应用、能力、任务四个管理页矩阵全部满足：全局标题栏名称与当前页面一致，主内容同名重复计数为 0，主内容没有第二条 Header。
- React 结构复核通过：两个组件保持命名导出，Hook 无条件调用，页面名称由现有 Zustand 状态直接派生，交互按钮与双击拖动边界未改变。
- `npm run build`、`go test ./...`、`go vet ./...` 与 `wails3 package` 全部通过，最终产物已更新为 `bin/aestival.app`。
- 已知非阻塞提示保持不变：Node.js 20.10.0 低于部分 CLI 包声明的最低版本；production 主 chunk 约 631 kB。

## 2026-07-26 15:25（Asia/Shanghai）— BE-001 第十四轮修复：最大化后保持最小尺寸约束

### 用户反馈与根因

- 用户确认标题栏双击最大化/恢复已生效，但最大化后直接从四角缩小窗口时可突破 960×640，一直缩到接近只剩红黄绿控件；普通状态下最小尺寸正常。
- 核对 Wails 3 alpha2.117 源码确认：`WebviewWindow.Maximise()` 会先调用 `DisableSizeConstraints()`；只有显式走 `UnMaximise()` 才调用 `EnableSizeConstraints()`。macOS 从 zoom 状态直接拖角时由 AppKit 自动恢复，不经过 Wails `UnMaximise()`，因此保存的最小尺寸没有重新应用。

### 修改

- `frontend/src/services/window-service.ts`：在 `ToggleMaximise()` 外使用 `try/finally`，无论最大化切换成功或失败都执行 `EnableSizeConstraints()`，恢复 `main.go` 中的 960×640 最小尺寸。
- 修复继续集中在窗口适配层，不修改 `InvisibleTitleBarHeight`、原生四边/四角缩放或前端拖拽区，避免回归此前的角落缩放抖动。
- 同步 `AGENTS.md`、窗口布局设计与 Wails alpha 风险记录，明确任何最大化/恢复路径都不能清空最小尺寸约束。

### 验证

- 本项目 `npm run build`、`go test ./...`、`go vet ./...` 与 `wails3 package` 全部通过，最终产物已更新为 `bin/aestival.app`。
- Wails 上游约束单元测试 `TestRestoreSavedSizeConstraintOptions`、`TestRestoreSavedSizeConstraintOptionsNoopWithoutSavedValues` 与 `TestDisableSizeConstraintsDoesNotOverwriteSavedValues` 通过，确认 `EnableSizeConstraints()` 会恢复此前保存的最小/最大尺寸。
- 源码链路复核：`ToggleMaximise → Maximise → DisableSizeConstraints` 后，本项目现在固定追加 `EnableSizeConstraints`；再次切回普通状态时重复启用约束为幂等操作。
- 已知非阻塞提示保持不变：Node.js 20.10.0 低于部分 CLI 包声明的最低版本；production 主 chunk 约 631 kB。原生连续拖角的最终手感和 960×640 停止点等待用户复核。

## 2026-07-26 15:11（Asia/Shanghai）— BE-001 第十三轮修复：标题栏双击最大化/恢复

### 用户反馈

- 用户要求补齐桌面软件的标准标题栏行为：双击标题栏放大窗口填充可用区域，再次双击恢复原尺寸。

### 修改

- 新增 `frontend/src/services/window-service.ts`，集中封装 Wails `Window.ToggleMaximise()`；浏览器预览无桌面运行时环境时安全跳过，展示组件不直接散落调用运行时 API。
- `frontend/src/components/shell/app-titlebar.tsx`：标题栏监听主按钮双击，空白拖动区调用最大化/恢复；命中 `.app-no-drag` 的侧栏、搜索、右侧栏和底部面板等真实按钮时直接退出，不触发窗口切换。
- 调用失败时使用 Sonner 给出“无法切换窗口大小”，未使用浏览器原生提示。
- 同步 `AGENTS.md` 和标题栏/全局交互设计，固化双击标题栏空白区的行为及 `no-drag` 排除边界。

### 验证

- 浏览器预览双击标题栏空白区后无文字选区、无异常弹层，标题栏继续保持 `--wails-draggable: drag`；全局搜索按钮仍正常打开 Command Dialog。
- 标题栏内可交互节点计算样式均为 `--wails-draggable: no-drag`，双击处理器使用同一 `.app-no-drag` 边界排除按钮。
- 最新 `bin/aestival.app` 原生窗口的系统 zoom 通道可在 1315×768、1329×768、1316×768 间切换并恢复，确认当前 Wails/AppKit 窗口支持可逆最大化；标题栏双击事件通过同一 `ToggleMaximise` API 接入，等待用户连续双击手感复核。
- `npm run build`、`go test ./...`、`go vet ./...` 与 `wails3 package` 全部通过，最终产物已更新为 `bin/aestival.app`。
- 已知非阻塞提示保持不变：Node.js 20.10.0 低于部分 CLI 包声明的最低版本；production 主 chunk 约 631 kB。

## 2026-07-26 14:36（Asia/Shanghai）— BE-001 第十二轮修复：标题栏拖动禁选与内容选择边界

### 用户反馈与原因

- 用户反馈标题栏显示文本选择光标，拖动起步略有迟滞，怀疑首次按下先进入文字框选。
- 此前完整标题栏虽然已有 Wails `drag` 语义，但应用没有统一的文本选择边界；标题栏也没有显式光标样式，因此文字节点附近仍可能反馈为文本选择。

### 修改

- `frontend/src/index.css`：应用外壳默认设置 `user-select: none`；标题栏拖动区同时设置 `cursor: default` 与禁选；输入、可编辑表面以及 `.app-selectable-content`/`data-app-selectable-content` 显式恢复文本选择。
- `frontend/src/components/shell/workspace-shell.tsx`：把主内容声明为可选择内容表面；shadcn `Button` 自带 `select-none`，建议卡等交互控件不会随主内容恢复文字框选。
- `frontend/src/components/shell/app-titlebar.tsx`：清理已注释标题遗留的未使用数据与状态读取，不改变当前渲染结果，并恢复 TypeScript production build。
- 同步 `AGENTS.md`、视觉/窗口布局、全局交互设计和 `DEC-011`，明确消息正文、文档/代码预览、输入、Monaco、终端和日志后续必须显式保留选区能力。

### 验证

- 浏览器 1280×720 计算样式矩阵：`body` 为 `user-select: none`；主内容和 textarea 为 `text`；标题栏为 `cursor: default`、`user-select: none`、`--wails-draggable: drag`；搜索按钮为 `pointer`/`no-drag`，建议按钮为 `pointer`/`none`。
- 输入框写入“选择能力验证”后执行 `⌘A`，选区范围为 0–6；清理测试内容后拖动标题栏，`window.getSelection()` 为空，画面无文字选区；1280×720 下文档尺寸与视口一致，无溢出。
- `npm run build`、`go test ./...`、`go vet ./...` 与 `wails3 package` 全部通过，最终产物已更新为 `bin/aestival.app`。
- 已知非阻塞提示保持不变：Node.js 20.10.0 低于部分 CLI 包声明的最低版本；production 主 chunk 仍大于 500 kB。
- 当前尚未接入真实 Monaco 与终端表面；本轮已建立显式 opt-in 边界，接入 UI-002/后续工作区任务时需按 `DEC-011` 复核组件自有选区。

## 2026-07-26 09:16（Asia/Shanghai）— BE-001 第十一轮修复：完整标题栏恢复窗口拖动

### 用户反馈与根因

- 第八轮角落缩放抖动已经由用户确认解决，但当前只能从标题栏上下很窄的区域移动窗口。
- Wails 运行时已经正确加载；真正的命中问题位于标题栏 DOM：左侧标题/空白容器使用 `flex-1` 占据几乎整行，左右两组大容器又整体声明为 `app-no-drag pointer-events-auto`，因此覆盖层只有容器上下的窄缝仍可命中 `drag`。

### 修改

- `frontend/src/components/shell/app-titlebar.tsx`：把 `app-drag-region` 直接放到 53px 完整标题栏，移除单独的绝对定位拖拽覆盖层。
- 大范围左右布局容器不再设置 `no-drag` 或恢复指针事件；仅侧栏开关、全局搜索、右侧栏和底部面板四个真实按钮使用 `app-no-drag pointer-events-auto`。
- `main.go`：同步窗口配置注释，明确四边/四角由 AppKit 原生缩放，前端仅负责标题栏拖动，未恢复 `InvisibleTitleBarHeight`。
- 保留第十轮 `frontend/src/main.tsx` 的 `@wailsio/runtime` 副作用引入，未改动任何业务服务。

### 验证

- 浏览器 1280×720 命中矩阵：标题栏左端、标题文字两侧、中部、右侧空白及 y=51 均为 `drag`；四个按钮均为 `no-drag`；y=55 已离开标题栏且不继承拖动。文档宽高与视口同为 1280×720，无溢出。
- 用户在本项目原生应用中实测确认整条标题栏可以移动窗口，问题已修复；第八轮四角缩放修复保持有效。
- `go test ./...`、`go vet ./...`、TypeScript/Vite production build 与 `wails3 package` 全部通过，最终产物已更新为 `bin/aestival.app`。
- 已知非阻塞提示：Node.js 20.10.0 低于部分 CLI 包声明的最低版本；production 主 chunk 仍大于 500 kB。
- 设计 QA：`docs/协作同步/design-qa-titlebar-drag-1280x720.jpg`；源图与实现的全屏/标题栏聚焦对照：`docs/协作同步/design-qa-titlebar-drag-comparison.png`。

## 2026-07-26 08:47（Asia/Shanghai）— BE-001 第十轮修复：加载 Wails 运行时，激活 CSS 拖拽路径

### 用户反馈与排查

- 第九轮拖拽面全覆盖后用户实测仍无法拖动窗口，说明问题不在死区，而是 CSS 拖拽路径整体未生效。
- 根因确认：前端从未 `import "@wailsio/runtime"`（`main.tsx` 与 `index.html` 均无引用），Wails vite 插件仅处理 typed events 解析、不注入运行时；因此 `drag.ts` 的鼠标监听从未注册，`--wails-draggable` 一直是死样式。此前所有窗口拖动能力实际全部来自 `InvisibleTitleBarHeight` 原生强制拖拽，第八轮移除后拖拽能力整体归零。

### 修改

- `frontend/src/main.tsx`：首行副作用引入 `@wailsio/runtime`（`dist/index.js` 内含 `drag.js` 副作用初始化，package.json 已标记 sideEffects 不会被 tree-shake）。
- 运行时附带的 contextmenu 模块已审查：仅在设置 `--custom-contextmenu` 时才接管右键，与现有全局禁用原生右键和 React ContextMenu 不冲突；macOS 上运行时的 JS 自定义边缘缩放不启用，缩放飞纯原生。

### 验证

- `npm run build` 通过（主 chunk 615.69 → 630.87 kB，为运行时体积）；`wails3 package` 通过，`bin/aestival.app` 内二进制已更新（2026-07-26 08:47）。
- 待用户验证：①标题栏任意位置（含上边缘）可拖动窗口；②四边与四角原生缩放正常、无抖动；③右键菜单与标题栏按钮交互不受影响。

## 2026-07-26 01:17（Asia/Shanghai）— BE-001 第九轮修复：标题栏拖拽面全覆盖，恢复上边缘拖动窗口

### 用户反馈与修改

- 第八轮移除 `InvisibleTitleBarHeight` 后抖动根治，但用户反馈无法从窗口上边缘拖动窗口。
- 原因：此前"顶部边缘可拖动"实际由 `InvisibleTitleBarHeight` 的原生强制拖拽区提供；CSS 拖拽面第四轮内缩 8px，导致 y∈[5,8) 成为既不可缩放也不可拖动的死区，移除强制拖拽后死区暴露。
- 机制核实：原生边缘/角落缩放手势从按下起由 AppKit 独占，缩放期间鼠标事件不送达 webview，CSS 拖拽不会在原生缩放中途触发（否则第八轮后角落缩放仍会抖，但用户已确认不抖）；因此拖拽面可安全覆盖整个标题栏。
- 修改 `frontend/src/components/shell/app-titlebar.tsx`：拖拽面由 `inset-x-2 top-2 bottom-0` 改为 `inset-0`，消除顶部与两侧死区；顶部约 5px 仍按 macOS 平台惯例为原生纵向缩放区。

### 验证

- `npm run build`、`wails3 package` 通过；`bin/aestival.app` 内二进制已更新（2026-07-26 01:17）。
- 待用户验证：①标题栏任意位置（含上边缘紧邻缩放区下方）可拖动窗口；②四边与四角原生缩放仍正常、无抖动；③侧栏边缘 rail、标题栏按钮交互不受影响。

## 2026-07-26 01:05（Asia/Shanghai）— BE-001 第八轮修复：移除隐形标题栏强制拖拽，根治顶部角落缩放抖动（用户已验证）

### 用户反馈与排查

- 第七轮的"缩放期间临时不透明"方案经用户实测无效并已回退。用户提供关键线索：从顶部角落缩到最小尺寸后继续移动鼠标，窗口会往对角"跑位"；抖动表现类似窗口先跑向对角再被拉回。
- 根因确认（Wails v3.0.0-alpha2.117 源码）：全局 `NSEventMaskLeftMouseDown` 监听在每次左键按下时调用 `handleLeftMouseDown:`，只要落点在 `InvisibleTitleBarHeight`（46px）顶部区域内且距左右边缘超过 5px，就立即强制执行 `performWindowDragWithEvent`（移动窗口）。现代 macOS 的角落/边缘缩放手柄大于 5px（Wails 对 issue #4960 的 5px 规避不足），顶部角落按下时 AppKit 同时启动实时缩放：一个模态循环移动窗口、一个缩放窗口，每帧互相拉扯 → 抖动；缩到最小尺寸后缩放钳制但拖拽仍在移动 → 窗口跑位。底部角落不在顶部 46px 区域内，无冲突，因此始终流畅。
- 运行时 JS（`drag.ts`）核实：macOS 上 JS 自定义边缘缩放不启用，缩放为纯原生；CSS `--wails-draggable: drag` 拖拽面经 `wails:drag` → `startDrag()` 独立路径工作，不依赖 InvisibleTitleBarHeight。

### 修改

- `main.go`：移除 `InvisibleTitleBarHeight: 46`，保留 `MacBackdropTranslucent` 与 `MacTitleBarHiddenInsetUnified`（红绿灯位置由后者决定，不受影响）。窗口拖拽改由前端 CSS 拖拽面承担。

### 验证

- `go vet ./...`、`go test ./...`、`wails3 build`、`wails3 package` 通过。
- 用户实测确认：四个角落缩放抖动/跑位问题已解决（commit 82bed3d）。

## 2026-07-26 00:11（Asia/Shanghai）— UI-001 第六轮标注修复：侧栏边缘 hover 高亮贯穿标题栏

### 用户反馈与修改

- 用户反馈：鼠标悬停侧栏右缘时，标题栏下方区域出现高亮加粗线，但标题栏那一行没有高亮。
- 原因：`SidebarRail` 以 `-top-[50px]` 上延进入标题栏区域，但它位于内容行的 `z-10` 层叠上下文内，标题栏为 `z-20`；标题栏背景层遮住了 rail 上延段的 hover 高亮线，并拦截了该区域的鼠标事件。
- 修改 `frontend/src/App.tsx`：内容行容器由 `z-10` 调整为 `z-30`。行内容与标题栏垂直方向不重叠，唯一探入标题栏区域的元素是 rail 上延段（已全仓确认），因此仅 rail 上延段的绘制与事件层级移到标题栏之上。
- 效果：hover 高亮线从标题栏连续到底部；标题栏内 16px 侧栏边缘条恢复可 hover、可点击切换侧栏，符合第四轮"可拖拽/hover 边缘从标题栏顶部连续到底部"的意图。

### 验证

- `npm run build`（tsc + Vite production build）通过；仍仅有既知的主 chunk 体积提示。
- 全仓检索确认内容行内无其他负向 top 偏移元素；ContextMenu/DropdownMenu/Tooltip/HoverCard 均为 portaled 浮层，不受行层级调整影响。
- 侧栏收起滑出仍在内容行内进行，与标题栏无垂直重叠，不影响第五轮的同步回缩修复。

## 2026-07-26 00:02（Asia/Shanghai）— UI-001 第五轮标注修复：侧栏与标题栏回缩统一

### 用户反馈与修改

- 用户反馈：侧栏与其上方标题栏区域不是一体，收起侧栏时两者回缩不统一，视觉不一致。
- 原因：标题栏左段按侧栏状态在 `w-[var(--sidebar-width)]` 与 `flex-1` 之间切换并即时增删 `bg-background`/`border-b`，`flex` 与固定宽度之间无法插值，导致侧栏上方区域瞬间跳变，而侧栏与材质层以 200ms 滑出。
- 修改 `frontend/src/components/shell/app-titlebar.tsx`：标题栏改为固定单行布局，72px 安全区、侧栏切换、标题与右侧按钮位置不再随侧栏状态变化（符合设计方案 01 第 7 节）；实体背景与底边抽为独立背景层，其左缘在展开态位于 `var(--sidebar-width)`、收起态位于 0，以 `transition-[left] 200ms ease-linear` 与侧栏容器、全高材质层的滑出完全同步。
- 展开态视觉不变：左 256px 仍透出整列毛玻璃；收起末态不变：标题栏恢复全宽实体底边。

### 验证

- `npm run build`（tsc + Vite production build）通过；仍仅有既知的主 chunk 体积提示。
- 同步核对三处动画参数：侧栏容器 `transition-[left] 200ms ease-linear`、材质层 `transition-transform 200ms ease-linear`、标题栏背景层 `transition-[left] 200ms ease-linear`，整列右缘位置在任意时刻一致。
- 标题栏仍保留 z-20，SidebarRail 上延 50px 的事件层级与内缩 8px 拖拽面行为不变；`prefers-reduced-motion` 下动画照常降级。

## 2026-07-25 23:31（Asia/Shanghai）— UI-001 第四轮标注调整

### 用户反馈与修改

- 整列 Sidebar 材质层由 80% 侧栏色染色调整为 60%，并由固定定位改为应用根容器内的绝对定位；Wails 原生 `MacBackdropTranslucent` 继续负责毛玻璃模糊。
- Sidebar 的 tab 轨道、hover、选中和导航 active 状态全部使用半透明语义色重新分层：tab 轨道约 7%，hover 35%，选中 65%，导航 active 约 7%。
- `SidebarRail` 向上延伸 50px，使可拖拽/hover 边缘从标题栏顶部连续到底部；全高材质层仍统一绘制唯一的 1px 右边框。
- 标题栏不再整块声明为 Wails 拖拽区域，改为单独的内缩 8px 拖拽面；窗口顶部、左右边缘和左上角保留原生缩放安全区，交互按钮继续显式 `no-drag`。
- 应用根节点改用稳定的 `width/height: 100%`，移除 `100vh`/动态视口高度依赖；Sidebar 从 fixed 改为工作区内 absolute，降低从左上方向缩放时原生窗口坐标变化与 WebView 固定层重排相互竞争的风险。

### 验证

- 浏览器计算样式确认：全高材质层为 256 × 720、背景透明度 60%、单一 1px 右边框；`SidebarRail` 为 y=0–720，标题栏拖拽面从 x/y=8px 开始。
- Sidebar 展开/完全收起后无页面溢出；全局搜索可打开和关闭，任务输入可编辑并清空；浏览器控制台无 error/warning。
- 使用当前项目精确生成的 `bin/aestival.app` 检查原生红黄绿控件、整列毛玻璃、状态层次和边界，并执行左上/顶部与右下方向缩放；缩放后的布局稳定、无错位或溢出。Computer Use 不记录拖动过程每一帧，最终仍保留人工连续拖动复核项。
- React 变更复核通过：未新增条件 Hook、重复状态、无语义点击容器或不稳定列表 key，仍复用 shadcn Sidebar/Tabs。
- `npm run build`、`env GOCACHE=/tmp/aestival-go-cache go test ./...`、`env GOCACHE=/tmp/aestival-go-cache wails3 build`、`env GOCACHE=/tmp/aestival-go-cache wails3 package` 通过。
- 第四轮实现截图：`docs/协作同步/design-qa-sidebar-glass-states-1280x720.png`；原生窗口截图：`docs/协作同步/design-qa-sidebar-native-1327x768.jpg`；并排对照：`docs/协作同步/design-qa-sidebar-glass-states-comparison.png`。

## 2026-07-25 22:52（Asia/Shanghai）— UI-001、BE-001 第三轮标注调整

### 用户反馈与修改

- 新增单一的全高 Sidebar 材质层，从标题栏顶部贯穿到应用底部；标题栏左段和下方 Sidebar 内容层改为透明叠加。
- 全高材质层统一绘制 1px 右边框，下方 Sidebar 容器移除原有边框，消除标题栏处的断点和重复线。
- 移除 CSS `backdrop-filter`，保留 80% 侧栏语义色染色；模糊统一交给 Wails `MacBackdropTranslucent` 原生效果，降低移动窗口时的合成闪烁风险。
- macOS 隐形标题栏从 48px 调整为 46px，使原生红黄绿窗口控件再轻微上移。

### 验证

- 浏览器计算样式确认：全高材质层为 256 × 720、背景透明度 80%、仅一条 1px 右边框；标题栏左段和 Sidebar inner 均为透明背景。
- Sidebar 展开/完全收起无页面溢出，材质层与内容层同步移入/移出。
- 全局搜索和任务输入复测可用；浏览器控制台无错误。
- React 结构复核通过：背景层无交互、无条件 Hook、无额外状态同步和无障碍问题。
- `npm run build`、`env GOCACHE=/tmp/aestival-go-cache go test ./...`、`env GOCACHE=/tmp/aestival-go-cache wails3 build` 通过。
- 第三轮全屏对照：`docs/协作同步/design-qa-sidebar-glass-comparison.png`；侧栏聚焦对照：`docs/协作同步/design-qa-sidebar-glass-focused-comparison.png`。

## 2026-07-25 22:25（Asia/Shanghai）— UI-001 第二轮标注微调

### 用户反馈与修改

- Wails 隐形标题栏高度从 50px 调整为 48px，使 macOS 红黄绿窗口控件轻微上移；前端内容高度仍为 50px。
- 标题栏按侧栏宽度拆分：侧栏段移除底边，使用侧栏语义背景、80% 透明度和 24px 背景模糊。
- Wails 窗口背景改为透明 RGBA，并继续使用 `MacBackdropTranslucent`；侧栏收起后标题栏恢复连续白色底边。
- 输入组覆盖聚焦状态的边框色与 ring，聚焦输入时不再出现第二层描边。

### 验证

- 浏览器计算样式：侧栏标题栏宽 256px、底边 0px、`backdrop-filter: blur(24px)`；主标题栏保持 1px 底边。
- 侧栏完全收起后占位宽度为 0px，标题栏两段均恢复 1px 底边，无页面溢出。
- 输入框聚焦前后均为 1px `input` 边框，聚焦 ring 计算宽度为 0px。
- `npm run build`、`go test ./...` 与 `wails3 build` 通过；构建仍仅有既知的 Node engine 和主 chunk 体积警告。
- 第二轮设计对照：`docs/协作同步/design-qa-glass-adjustment-comparison.png`。

## 2026-07-25 22:04（Asia/Shanghai）— UI-001 标注调整

### 用户反馈与修改

- 移除标题栏前进/后退；搜索改为只显示图标。
- Wails 隐藏式标题栏与前端标题栏统一为 50px。
- 新建任务默认不再渲染“聊天”内容页签。
- Sidebar 改为完全离屏收起，收起后主内容占满窗口且无残留图标栏。
- 会话子列表移除左侧缩进竖线。
- 保留提供的 SVG 路径，移除软阴影滤镜与额外透明度，提高欢迎区和页脚小图标清晰度。
- 删除输入区上方独立模式/任务/Mock 信息行；修复禁用发送按钮导致整个输入区灰化和 50% 透明的问题。

### 验证

- 1440×900：展开态、完全收起态和展开会话态均完成视觉检查。
- 960×640：无整体溢出，输入区背景为白色且欢迎区不重叠。
- 全局搜索纯图标入口可打开 Command；任务输入可编辑并启用发送按钮。
- `wails3 build` 与 `env GOCACHE=/tmp/aestival-go-cache go test ./...` 通过。
- 调整后设计对照：`docs/协作同步/design-qa-adjustment-comparison.png`。

## 2026-07-25 21:40（Asia/Shanghai）— UI-001、BE-001

### 变更文件

- 前端基础：`frontend/package.json`、`frontend/package-lock.json`、`frontend/vite.config.ts`、`frontend/tsconfig*.json`、`frontend/components.json`、`frontend/src/index.css`、`frontend/src/components/ui/`
- 应用实现：`frontend/src/App.tsx`、`frontend/src/components/shell/`、`frontend/src/components/chat/`、`frontend/src/components/pages/`、`frontend/src/components/panels/`、`frontend/src/store/`、`frontend/src/data/`、`frontend/src/hooks/`
- 桌面外壳：`main.go`、`go.mod`、`build/Taskfile.yml`
- 设计验收：`design-qa.md`、`docs/协作同步/design-qa-implementation-1440x900.png`、`docs/协作同步/design-qa-comparison.png`

### 主要内容

- 建立 Tailwind CSS v4、Nova/Base UI 风格的 shadcn/ui 组件层、Geist 字体、Lucide 图标与 Zustand 页面状态。
- 实现全宽桌面标题栏、三段式 Sidebar、代理/聊天模式、固定任务项目、全局 `Command + Dialog`、区域 `ContextMenu` 和 Mock 页面导航。
- 实现 Codex 风格的新建任务首屏、四个建议动作、复合输入区与诚实的 Mock 反馈。
- 宽窗口使用 `Resizable` 右侧栏和底部面板；窄窗口右侧栏降级为 `Sheet`，短窗口隐藏建议动作以避免输入区重叠。
- 配置 Wails 主窗口名称、默认/最小尺寸、macOS 隐藏式统一标题栏、标题栏安全区与背景；未注册业务服务。

### 验证

- `npm run build`：通过；Vite 6.4.3 production build 完成。
- `env GOCACHE=/tmp/aestival-go-cache go test ./...`：通过。
- `env GOCACHE=/tmp/aestival-go-cache wails3 build`：通过；生成 `bin/aestival`。
- 浏览器 1440×900：页面宽高与滚动宽高一致，无整体溢出。
- 浏览器 960×640：页面宽高与滚动宽高一致，欢迎区和输入区不重叠。
- 全局搜索筛选与导航、窄窗口右侧工作区、Mock 输入反馈均完成交互复测。
- 设计 QA：`design-qa.md` 最终结果为“通过”；无 Card 嵌套或卡片墙。

### 限制与下一步

- 管理页仍为页面骨架，未接入业务数据。
- production bundle 有大于 500 kB 的警告；后续按页面和能力域拆包。
- 下一项可直接执行任务：UI-002。
