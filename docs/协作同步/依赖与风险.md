# Aestival 依赖与风险

> 最后更新：2026-08-12 00:53
> 时区：Asia/Shanghai

## 1. 依赖

| ID | 依赖 | 状态 | 影响 | 解除条件 |
| --- | --- | --- | --- | --- |
| DEP-001 | shadcn 4.14.1 CLI 要求 Node.js ≥ 20.18.1，当前环境为 20.10.0 | 不阻塞当前实现 | 已生成组件和 production build 可用；后续再次运行 shadcn CLI 时会出现 engine 警告 | 将本地 Node.js 升级到 20.18.1 以上或当前 LTS |
| DEP-002 | DOC-005 规划的 Markdown/KaTeX/Mermaid/Shiki/sanitize 与聊天 shadcn 组件 | 已解除（UI-014） | 已安装依赖并加入官方聊天组件；当前仍只服务前端 Mock，不接真实模型或文件服务 | UI-014 完成后锁定渲染安全样例和构建回归证据 |
| DEP-003 | UI-022 浏览器 PDF 解析依赖 `pdfjs-dist` 4.10.38；Office 样例产物生成依赖开发机 LibreOffice、openpyxl、python-pptx 与 pypdf，PPT 保真产物可额外使用 PowerPoint/Keynote 原生导出 | 不阻塞当前实现 | 应用运行只依赖已提交产物和懒加载 PDF.js；重新生成或替换 Office 样例时需要对应开发工具，LibreOffice 环境缺少 CJK 字体时不能单独保证 PPT 中文 | 后续 Wails 文档服务提供统一转换能力，在 CI 或受控构建环境固定转换工具与字体，并把缺字体作为显式错误 |
| DEP-004 | UI-023 依赖 DOC-006 的用户验收 | 已解除（2026-08-11 23:00） | 用户已明确要求依据文档进入实现；UI-023 已按边界完成前端 Mock 并在 2026-08-11 23:53 转为待验收 | 已验证未接真实 Feed 抓取、OPML 文件读写、模型分类、持久化或其他 Go 业务；后端能力必须另建任务 |
| DEP-005 | UI-024 依赖 DOC-007 的用户验收 | 已解除（2026-08-11 23:59） | 用户已明确要求依据文档进入实现，DOC-007 已完成评审，UI-024 已于 2026-08-12 00:53 转为待验收 | 已按文档完成前端 Mock 和验证；真实项目、文件与索引服务必须另建任务 |
| DEP-006 | 笔记编辑与图谱依赖 Vditor、Cytoscape.js 及本地动态资源 | 已解除（UI-024） | 已锁定 Vditor 3.11.3 与 Cytoscape.js 3.34.1；两者仅在笔记编辑/图谱打开时从本地资源动态加载，Vditor 静态资源由 Vite 本地服务与 production 复制插件提供，不使用 CDN | 继续在桌面三平台验收 Wails WebView 生命周期和性能；升级依赖时复测四模式、重复挂载/卸载、双编辑组、图谱列表与 production 资源路径 |


## 2. 风险

| ID | 风险 | 级别 | 影响 | 缓解方式 |
| --- | --- | --- | --- | --- |
| RISK-001 | 当前前端 production 主 chunk 约 1.17 MB，Monaco 环境按需 chunk 约 4.03 MB，Cytoscape 按需 chunk 约 444 kB | 中 | 首屏和首次打开文件、应用、Skill 编辑器、统计或笔记图谱时可能出现加载等待；语言 Worker 也会在需要时加载 | Monaco、Cytoscape、Vditor、应用、能力、任务和设置页均保持本地按需加载，不依赖 CDN；后续在 QA-002 单独评估主界面公共代码、Monaco/Recharts 手动分包、笔记资源裁剪与空闲预热，不以牺牲离线能力换取体积 |
| RISK-002 | Wails 3 仍使用 alpha 版本 | 中 | 窗口 API 或平台行为可能随升级变化；当前 `Maximise()` 会主动禁用尺寸约束，macOS 原生拖角恢复又可能绕过 `UnMaximise()` | 窗口配置集中保留在 `main.go`；前端窗口适配层在最大化切换后显式恢复约束；升级前执行三平台窗口、标题栏和最小尺寸回归 |
| RISK-003 | 自动化原生窗口检查只能捕获缩放后的画面，不能逐帧量化拖动中的微抖 | 低 | 极短促的缩放抖动无法由最终截图发现 | 已关闭（2026-07-26）：第八轮移除 `InvisibleTitleBarHeight` 后用户实测确认角落缩放抖动根治；第十一轮收窄 `no-drag` 命中范围后，用户再次确认完整标题栏拖动正常且缩放修复无回归 |
| RISK-004 | Mermaid、Shiki 和 KaTeX 可能明显增大首屏或聊天首开体积 | 中 | 聊天打开或首次遇到代码/图表时可能出现加载延迟，影响流式连续性 | 已按需动态导入 Mermaid/Shiki；构建仍报告约 698 kB 聊天页面 chunk 与若干图表语言 chunk，继续在用户验收后评估手动分包和首批语言限制 |
| RISK-005 | `rehype-raw` 与 Mermaid SVG 会扩大不受信任内容的渲染面 | 高 | HTML、SVG、链接或 Mermaid 指令处理不严可能造成脚本执行、外部资源加载或布局破坏 | 已使用显式 HTML/SVG 白名单、危险协议过滤、固定 Mermaid `securityLevel: "strict"`、禁止事件属性和外部资源；用户验收继续覆盖恶意 HTML、协议和 SVG 样例 |
| RISK-006 | Monaco 完整编辑服务、全量基础语言贡献和 TypeScript Worker 会显著增加编辑器资源体积 | 中 | UI-020 production build 中 Monaco 环境 chunk 约 4.03 MB（gzip 约 1.04 MB），TypeScript Worker 约 7.04 MB；低配机器或首次打开编辑器时可能出现加载等待 | 编辑工作台整体保持按需加载，语言定义按文件首次使用加载，Worker 与主界面分离；为保证 Suggest、Diff 和编辑命令服务完整，本轮不再使用会导致服务缺失的局部 API 导入。后续单独评估 Monaco 手动分包、空闲预热和性能埋点，不以牺牲语言覆盖或编辑服务为代价 |
| RISK-007 | 项目看板首版使用内存 Mock，未来 Wails 持久化与 AI 并发规划可能产生版本冲突或重复命令 | 中 | 刷新会重置工作项；接入真实 AI 后，人类拖放、AI 规划和多表面操作可能同时修改顺序、状态或日期 | 展示组件只通过统一 `ProjectBoardCommand`/策略/服务提交变更并记录审计；未来 Adapter 增加版本号、幂等命令 ID、乐观并发检查和失败回滚，继续在服务层强制人工验收与作废权限 |
| RISK-008 | Office 首版依赖已提交的样例预览产物，不能在运行时打开任意新 Word/PPT/Excel 文件 | 中 | 未登记文件只能显示不可预览；源文件变化后旧产物可能失效，跨平台字体会改变分页，LibreOffice 缺少演示文稿引用的中文字体时甚至会生成中文空白的 PDF | 清单记录源文件摘要、页数/工作表数和产物路径；PPT 样例已改用原生 PowerPoint 导出的保真 PDF，生成脚本支持 `--presentation-preview` 并校验产物存在。未命中清单时明确失败且不访问外部服务；未来 Wails Adapter 按源摘要缓存并报告缺字体或转换错误 |
| RISK-009 | 阅读分类使用远程模型时会把文章正文/摘要、元数据、用户要求和当前合集反馈规则发送给第三方模型供应商 | 高 | 用户可能在不知情时发送受版权、保密或个人偏好约束的内容；供应商留存策略、跨境处理或模型故障也可能扩大暴露面 | UI-023 已实现首次授权披露、本地模型选择、按模型授权与撤销入口，并明确不发送 Feed 凭据；当前只是内存 Mock，不发送任何文章。真实接入仍须按单篇、单合集最小化载荷，避免普通 Zustand、日志和导出保存完整请求/响应，并在后端设计阶段单独评审供应商策略与保留期 |
| RISK-010 | 未来 Feed 抓取器将处理不受信任 URL、重定向、XML/JSON/HTML 和文章提示文本 | 高 | 可能发生 SSRF/云元数据或内网访问、DNS 重绑定、重定向绕过、超大/压缩炸弹响应、XXE/实体扩展、解析器耗尽、脚本/危险链接执行，或文章提示注入诱导分类模型越权 | UI-023 只生成固定候选和 Feed 正文/摘要样例，不发起网络抓取，也不给文章工具或副作用权限。未来仅允许受控 `http/https`；初始 URL 和每次重定向都阻止回环/私网/保留/云元数据地址，限制重定向、时长、并发、响应与解压大小，禁用 DTD/外部实体、严格清洗 HTML，并在后端实现前单独完成威胁建模与安全测试 |
| RISK-011 | Vditor 与 Cytoscape.js 会增加笔记首开包体，Vditor DOM/回调和 Cytoscape 布局在 React 多编辑组/Strict Mode 下可能泄漏或争用宿主 | 中 | Cytoscape production chunk 约 444 kB（gzip 约 142 kB），Vditor 完整本地静态资源约 23 MB；首次打开笔记/图谱、重复初始化或 Buffer 回调环可能带来加载延迟、内容跳动或内存增长 | UI-024 已使用本地动态加载、挂载取消、实例销毁、关闭 Vditor 缓存、共享 Buffer 版本同步和 Cytoscape 卸载清理，并通过双编辑组/项目切换/图谱开关浏览器回归；仍需在 Wails 三平台进行长时间反复挂载和内存验收，并单独评估可安全裁剪的 Vditor 资源 |
| RISK-012 | 多根目录使用 `rootId + relativePath` 建模时，大小写、符号链接、移动/重命名、重名标题和 WikiLink 模糊解析可能造成越界、错链或图谱不完整 | 高 | 代理可能访问根目录外文件；反向链接或图谱可能把同名笔记连接到错误目标；partial 索引若被标为完整会误导用户 | 路径身份和根目录边界由 Adapter 规范化并在每次动作前验证真实父路径/符号链接；禁止父子根目录重叠；链接按显式根目录/相对路径、同根唯一、跨根唯一的顺序解析，歧义与未解析不建边；索引/图谱携带版本与完整度，移动重命名通过原子事件更新；真实服务前完成跨平台路径与越界安全测试 |


## 3. 更新规则

- 依赖解除后更新状态和解除证据。
- 风险消失时标记“已关闭”，不要删除历史。
- 新风险必须包含影响和至少一个可执行缓解动作。
- 阻塞任务需同时在任务看板中标记“阻塞”。
